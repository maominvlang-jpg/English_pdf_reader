# 英语课本 PDF 音频文本提取与点读工具（PRD）

## 1. 产品概述

### 1.1 背景与问题

- 英语课本 PDF 常见需求：提取英文句子/单词、中文翻译、英式发音点读、批量导出结构化数据与音频素材。
- 现有常见流程依赖多工具切换（截图/OCR/翻译/音频/表格），耗时且易错。

### 1.2 产品定位

- 纯前端（浏览器本地）运行的工具：
  - 本地加载 PDF 并渲染页面
  - 使用大模型进行 OCR（仅英文）与中文翻译
  - 右侧列表点击播放英式发音
  - 一键识别全书并缓存，翻页快速查看结果
  - 一键导出 Excel（.xlsx）并生成英文音频（.mp3），打包下载（.zip）

### 1.3 目标用户

- 英语教师/教研人员
- 家长（陪读/跟读）
- 教辅内容生产与运营人员（批量数据与音频素材）

## 2. 目标与成功指标

### 2.1 产品目标（Goals）

- G1：单页 OCR+翻译可用，结果可编辑（排序），可播放英式发音。
- G2：整本书一键识别后，翻页可快速命中缓存展示结果。
- G3：一键导出 Excel + mp3 音频素材，并打包下载。
- G4：保证英文识别纯净（过滤数字/中文/非英文）。

### 2.2 成功指标（Metrics）

- OCR+翻译成功率 ≥ 95%（以“可读英文文本块”计）。
- 一键识别完成后翻页缓存命中率 ≥ 99%。
- 导出成功率 ≥ 98%（受网络/浏览器策略影响时以可提示为准）。
- 导出字段正确率（页码/顺序/文本/翻译/音频引用）≥ 99%。

## 3. 范围定义

### 3.1 In Scope（包含）

- PDF 本地上传与页面渲染
- 单页 OCR（仅英文）+ 中文翻译
- 右侧结果列表展示（英文 + 中文）
- 右侧结果拖拽排序
- 点击播放（仅右侧播放入口）
- 一键识别全书（逐页处理）并缓存
- 生成数据：导出 Excel（.xlsx），生成对应 mp3 音频文件并打包 zip 下载
- API Key 本地保存（localStorage），状态显示“已配置/未配置”

### 3.2 Out of Scope（不做）

- 在 PDF 上绘制框选/序号标记（仅保留右侧列表）
- 多语言翻译（除中文外）
- 云端账号体系、云端存储、协作
- 细粒度文本编辑（逐字纠错、合并/拆分 bbox 等）
- 移动端深度适配（保证基本可用即可）

## 4. 用户画像与典型场景

### 4.1 用户画像

1. 教师用户：快速生成“句子+翻译+音频”素材用于课堂或作业。
2. 家长用户：孩子跟读训练，强调操作简单与英式发音。
3. 内容生产用户：需要稳定字段的结构化导出，便于导入点读系统/小程序。

### 4.2 用户旅程

- 旅程 A：单页点读
  - 保存 OpenRouter Key/Model → 选择 PDF → 识别当前页 → 右侧播放与排序 → 翻页继续
- 旅程 B：整书批量 + 导出
  - 选择 PDF → 一键识别全部（后台执行）→ 翻页秒看缓存 → 生成数据包（zip）

## 5. 信息架构与页面结构

### 5.1 页面模块

- 顶部控制区
  - API 配置：Key、Model、保存、配置状态
  - 文件与分页：选择 PDF、上一页/下一页、页码
  - 操作按钮：识别当前页、一键识别全部、生成数据包
- 左侧：PDF 渲染画布
- 右侧：OCR 列表（拖拽排序 + 播放）

## 6. 功能需求（FRD 级别）

### 6.1 PDF 上传与渲染

**功能点**

- 支持选择本地 PDF（上限 500MB）
- 成功加载后显示页码与翻页按钮
- 翻页渲染正确

**规则**

- 未加载 PDF：翻页、识别、导出按钮置灰
- 加载失败：显示错误提示，允许再次上传

**验收**

- 上传后可渲染第 1 页
- 翻页渲染稳定正确

### 6.2 API 配置（OpenRouter）

**功能点**

- 用户输入 OpenRouter API Key 与 Model
- 点击保存写入 localStorage
- 页面显示“已配置/未配置”
- Key 不回显（输入一次即可）

**规则**

- 未配置 Key：点击 OCR/翻译/一键识别/生成数据应提示配置 Key
- Model 可选，不填使用默认值

**验收**

- 刷新页面仍显示“已配置”
- 清理缓存后显示“未配置”

### 6.3 单页 OCR（仅英文）与翻译

**功能点**

- 点击“识别当前页”
- 当前页生成图片输入 → OCR 返回文本块与 bbox（内部使用）
- 每个文本块中文翻译
- 右侧展示：序号、英文、中文

**规则**

- OCR 文本过滤：仅保留英文单词/句子；忽略数字、中文与非英文文本
- 翻译失败：单条兜底展示（如“自动翻译失败”）
- 识别中：按钮 loading + 禁止重复点击

**验收**

- 同页多段文本可识别并展示
- 英文输出无中文/数字污染

### 6.4 右侧列表：拖拽排序

**功能点**

- 拖拽改变条目顺序
- 顺序变化后序号实时更新
- 翻页回来仍保持该页排序（按页缓存）

**规则**

- 拖拽中提供可视化反馈
- 支持拖到列表空白处（默认追加到末尾）

**验收**

- 任意两项可互换，序号与显示顺序一致

### 6.5 音频播放（英式发音）

**功能点**

- 右侧每条英文提供播放入口
- 播放优先选择 en-GB 声线（浏览器本地 TTS）
- 播放中点击其他条目可打断并播放新条目

**规则**

- 只保留右侧播放，不支持点击 PDF 播放

**验收**

- 点击任意条目能播放
- 发音偏英式（可感知差异时优先）

### 6.6 一键识别全部（批量 + 缓存）

**功能点**

- 点击“一键识别全部”后从第 1 页到最后一页逐页 OCR+翻译
- 识别结果缓存后翻页直接展示

**规则（本 PRD 选择）**

- 一键识别过程中：允许用户翻页操作
- 当前页展示逻辑：
  - 已缓存：直接展示缓存
  - 正在处理：显示“本页正在识别中…”
  - 未处理：显示“本页尚未识别…”
- 冲突策略：
  - 若用户在批处理中手动点“识别当前页”：以手动识别结果覆盖缓存；后台遇到该页时跳过
- 失败策略：
  - 某页失败：记录失败页并继续后续页；最终提示失败页列表（可简化为计数）
- 进度展示：当前页/总页数

**验收**

- 批处理期间翻页不影响后台任务继续执行
- 已识别页翻页秒出结果；未识别页提示状态明确

### 6.7 生成数据包（Excel + mp3 + zip）

**功能点**

- 点击“生成数据包”：
  - 汇总缓存识别结果（按页码升序）
  - 生成 Excel：`英语课本数据_YYYYMMDDhhmmss.xlsx`
  - 字段：页码、同页内顺序、音频序号、内容、中文翻译、英文音频
  - 为每条英文生成 `audio_序号.mp3`
  - 生成 zip：`英语课本数据_YYYYMMDDhhmmss.zip`，包含 xlsx 与 `/audio/*.mp3`

**规则（本 PRD 选择）**

- “英文音频”列：音频生成失败时单元格留空；zip 中不包含该音频文件
- 导出结束提示：成功/失败统计（至少提示失败条数）

**验收**

- zip 可下载并解压，目录结构正确
- xlsx 可打开，字段齐全
- mp3 可播放且与行内容对应（按音频序号）

## 7. 非功能需求（NFR）

### 7.1 性能

- 单页渲染：尽量 < 1s（受 PDF 复杂度影响）
- 单页 OCR+翻译：可接受 3–15s（受网络与模型影响）
- 一键识别：必须提供可感知进度，避免“假死”
- 导出：必须提供进度提示

### 7.2 可靠性

- 网络失败/限流：有明确提示，不应导致页面崩溃
- 429：指数退避重试；建议提示“正在重试”

### 7.3 安全与隐私

- Key 仅存本机 localStorage，不上传自有服务器
- 不在日志/控制台输出 Key
- 明示：OCR 需将页面图像发送给模型服务

### 7.4 兼容性

- 桌面 Chrome/Edge 优先
- Safari：允许存在差异但需基本可用（尤其下载策略与 TTS）

## 8. 异常与边界场景

- PDF 超大（页数很多）：一键识别耗时长，可能触发限流
- OCR 返回空：提示“未识别到文本”
- 翻译部分失败：单条兜底，不影响其他条目
- 导出被浏览器拦截下载：提示用户允许下载
- 刷新页面缓存丢失：本 PRD 要求“缓存持久化”，见下一节

## 9. 关键决策（已确认）

### 9.1 “英文音频”列失败策略

- 音频生成失败：Excel 对应单元格留空；zip 不生成占位文件；导出提示失败数。

### 9.2 一键识别过程允许翻页

- 一键识别为后台任务，用户可自由翻页查看已识别结果，不阻塞阅读。

### 9.3 缓存持久化（刷新后仍保留一键识别结果）

**目标**

- 刷新页面后仍可读取已完成的一键识别结果，避免重复 OCR/翻译请求。

**存储介质**

- 使用 IndexedDB 做结构化缓存；localStorage 仅存 Key/Model 等配置。

**PDF 唯一标识（pdfId）**

- 推荐方案：`fileName + size + lastModified` 组合生成 pdfId（可再做哈希编码）。

**建议数据结构**

- `books`：`pdfId, meta(fileName/size/lastModified/totalPages/createdAt/updatedAt)`
- `pages`：`pdfId, pageNum, items[{order,text,translation,bbox}], status(pending|done|failed), updatedAt`
- `jobs`（可选）：`pdfId, currentPage, totalPages, running, updatedAt`

**刷新后行为**

- 用户重新选择同一本 PDF（命中 pdfId）：
  - 直接读取缓存并展示已识别页结果
  - 若存在未完成任务记录：提示“是否继续识别”（可选）

**清理策略**

- 提供“清空当前 PDF 缓存”入口（强建议）
- 可选：只保留最近 N 本书（LRU）或按容量阈值清理

**验收**

- 完成一键识别后刷新页面，再选择同一本 PDF：已识别页无需再次调用 OCR/翻译即可展示结果。
- 清空缓存后刷新：不再命中旧结果。
- 多本 PDF 缓存互不串扰。

## 10. 验收清单（关键用例）

- 上传 PDF 并正常翻页渲染
- 单页识别：英文纯净（无中文/数字），翻译可见
- 右侧拖拽排序：顺序与序号一致
- 右侧播放：英式 TTS 可播放、可打断
- 一键识别：后台执行 + 允许翻页 + 进度展示 + 缓存命中展示
- 生成数据包：zip 中包含 xlsx 与 mp3；音频失败时单元格留空并提示失败数

